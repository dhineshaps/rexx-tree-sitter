"# rexx-tree-sitter" 

#To Install the Tree Sitter 
1)Node.js is required
2)tree-sitter works on C, so Visual Studio Code C++ tool are required https://visualstudio.microsoft.com/visual-cpp-build-tools/ , in Tools required needs beow to be enabled
  -> MSVC v143 – VS 2022 C++ x64/x86 build tools
  -> Windows 10 or 11 SDK
  -> C++ CMake tools for Windows (optional but good)

3)npm install -g tree-sitter-cli

4)tree-sitter --version

5)tree-sitter init -> to create structure



